import 'package:flutter/material.dart';

class AnimationSampleScreen extends StatelessWidget {
  const AnimationSampleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
